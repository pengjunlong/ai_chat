<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 主题：深色 + 无 ActionBar（使用底部导航替代） -->
    <style name="Theme.AndroidSample" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorOnSecondary">@color/on_secondary</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/status_bar</item>
        <item name="android:navigationBarColor">@color/bottom_nav_background</item>
        <!-- 全屏内容延伸到状态栏下方 -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- 启动页主题（防止白屏闪烁） -->
    <style name="Theme.AndroidSample.Splash" parent="Theme.AndroidSample">
        <item name="android:windowBackground">@color/background</item>
    </style>
</resources>

